name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        goos: [linux, windows, darwin]
        goarch: [amd64, arm64]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
      - name: Build
        run: |
          mkdir -p dist
          GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} go build -o dist/digispark-${{ matrix.goos }}-${{ matrix.goarch }} ./cmd/digispark/main.go
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: digispark-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/digispark-${{ matrix.goos }}-${{ matrix.goarch }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/**/digispark-*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
